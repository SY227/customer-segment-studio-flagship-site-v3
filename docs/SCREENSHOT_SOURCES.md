# Screenshot sources

All displayed product screens were extracted from the user-provided file:

`customer-segment-studio(1).mp4`

The source recording is 3590 × 2308 at 60 fps and 18.63 seconds long.

## Frames used

- `living-map-growing-buyers.*` — approximately 00:00.5
- `living-map-occasional-buyers.*` — approximately 00:02.5
- `living-map-dormant-vips.*` — approximately 00:08.5
- `customer-segment-cards.*` — approximately 00:18.0

Only the browser chrome and a very small outer edge were cropped. The product UI itself was not rebuilt, composited, or replaced.

The screenshots show the current product's demo data, which is also visible in the interface.

## V2.1 same-case walkthrough

The three product tabs now use the original `living-map-dormant-vips.png` and two direct crops of that exact 3488×2118 image:

- `assets/story/dormant-vips-context.*`: pixel box (2225, 457) to (3438, 1208).
- `assets/story/dormant-vips-action.*`: pixel box (2225, 1183) to (3438, 2088).

No numbers or interface elements were changed. The source shows 64 customers, 13% revenue share and $161,200. The website describes that dollar amount as historical demo contribution; the original screenshot's “Revenue at stake” label is not silently edited or treated as a forecast.

The hero animation is explicitly a workflow illustration, not a recording of those demo records being processed. It uses abstract rows without fabricated sales totals.
